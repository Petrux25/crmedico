from django.urls import path
from . import views

app_name = 'reservations'
urlpatterns = [
    # path('', views.search, name="search"),
    # path('<int:page_number>', views.result, name="result"),
]
